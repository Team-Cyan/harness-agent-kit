# Project Overview

## What This Project Is

Describe the product in a few bullets.

## What It Is Not

Describe explicit non-goals.

## Core Runtime Surfaces

- CLI / app entrypoint:
- config:
- state:
- audit:

## Current Architecture

Summarize the major loops or subsystems.

## Documentation Strategy

- root `AGENTS.md` for entry routing
- `.agents/` for repo-local agent assets and reusable templates
- `docs/ai/` for reusable knowledge
- `docs/roadmap.md` for current state
- `docs/specs/` for durable design decisions
- `docs/plans/` for implementation sequencing
- `docs/operations/` for operator procedures
